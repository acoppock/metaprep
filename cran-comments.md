## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new release.

## Test environments

* local macOS, R 4.x (R CMD check --as-cran): OK

## Downstream dependencies

There are no downstream dependencies (new package).
