% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{rescale_estimates_vcov}
\alias{rescale_estimates_vcov}
\title{Sign-flip or rescale an estimates_vcov object}
\usage{
rescale_estimates_vcov(ev, by)
}
\arguments{
\item{ev}{An \code{estimates_vcov} object.}

\item{by}{A per-estimate multiplier: a bare column name, an expression
evaluated in the estimates, or a numeric vector of length 1 (recycled) or
\code{nrow(estimates)}. Use \code{+1} / \code{-1} to flip signs, positive values to rescale.}
}
\value{
An \code{estimates_vcov} object with \code{estimate} (and \code{std.error},
\code{statistic}, \code{conf.low}, \code{conf.high} when present) and the vcov rescaled.
}
\description{
Multiply each estimate by a per-row factor and update the variance-covariance
matrix to match, keeping the object internally consistent. Use it to align the
sign of estimates across studies (\code{by} of \code{+1} / \code{-1}) or to change units
(e.g. \code{by = 100} for percentage points).

This is the correct way to transform estimate \emph{values}. The dplyr methods keep
the vcov row-aligned (subsetting, reordering) but never transform it, so
\code{mutate(estimate = -estimate)} would flip the estimates while leaving the vcov
(and its cross-study covariances) inconsistent. \code{rescale_estimates_vcov()}
applies \eqn{V \mapsto \mathrm{diag}(s)\, V\, \mathrm{diag}(s)}, so the
covariances stay valid, including the sign of cross-covariances under a partial
sign flip. \code{std.error}, \code{statistic}, and the confidence bounds are updated to
match when present.
}
\examples{
\dontshow{if (requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)
library(randomizr)
library(estimatr)

set.seed(123)
dat <- data.frame(Z = complete_ra(120, num_arms = 3), Y = rnorm(120))
ev <- as_estimates_vcov(bind_rows(
  study_1 = prep_fit(lm_robust(Y ~ Z, dat), term = c("ZT2", "ZT3")),
  .id = "study"
))

# Flip the sign of the first arm only; the cross-covariance sign updates too
ev |> rescale_estimates_vcov(by = if_else(term == "ZT2", -1, 1))

# Rescale to percentage points
ev |> rescale_estimates_vcov(by = 100)
\dontshow{\}) # examplesIf}
}
