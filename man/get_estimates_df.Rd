% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{get_estimates_df}
\alias{get_estimates_df}
\title{Extract estimates from prepped fits or estimates_vcov object}
\usage{
get_estimates_df(x, ...)
}
\arguments{
\item{x}{Either a prepped_fits tibble or an estimates_vcov object}

\item{...}{Additional arguments passed to methods}
}
\value{
A tibble of coefficient estimates
}
\description{
Generic function to extract estimates. Works with both:
\itemize{
\item \code{prepped_fits} tibbles (unnests tidy_obj)
\item \code{estimates_vcov} objects (extracts $estimates)
}
}
\examples{
\dontshow{if (requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(randomizr)
library(estimatr)

set.seed(123)
dat <- data.frame(Z = complete_ra(100, num_arms = 2), Y = rnorm(100))
fit <- lm_robust(Y ~ Z, data = dat)
prepped <- prep_fit(fit, term = "ZT2")
get_estimates_df(prepped)

ev <- as_estimates_vcov(prepped)
get_estimates_df(ev)
\dontshow{\}) # examplesIf}
}
