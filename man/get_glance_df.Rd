% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{get_glance_df}
\alias{get_glance_df}
\title{Extract glance summary from prepped fits or estimates_vcov object}
\usage{
get_glance_df(x, ...)
}
\arguments{
\item{x}{Either a prepped_fits tibble or an estimates_vcov object}

\item{...}{Additional arguments passed to methods}
}
\value{
A tibble of model-level statistics (or NULL for estimates_vcov)
}
\description{
Generic function to extract model-level summaries. Works with:
\itemize{
\item \code{prepped_fits} tibbles (unnests glance_obj)
\item \code{estimates_vcov} objects (not applicable - returns NULL with warning)
}
}
\examples{
set.seed(123)
dat <- data.frame(Y = rnorm(100), Z = sample(c("T0", "T1"), 100, TRUE))
prepped <- prep_fit(lm(Y ~ Z, data = dat), term = "ZT1")
get_glance_df(prepped)

}
