% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{get_glance_df}
\alias{get_glance_df}
\title{Extract glance summary from prepped fits or estimates_vcov object}
\usage{
get_glance_df(x, ...)
}
\arguments{
\item{x}{Either a prepped_fits tibble or an estimates_vcov object}

\item{...}{Additional arguments passed to methods}
}
\value{
A tibble of model-level statistics (or NULL for estimates_vcov)
}
\description{
Generic function to extract model-level summaries. Works with:
\itemize{
\item \code{prepped_fits} tibbles (unnests glance_obj)
\item \code{estimates_vcov} objects (not applicable - returns NULL with warning)
}
}
\examples{
\dontshow{if (requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(randomizr)
library(estimatr)

set.seed(123)
dat <- data.frame(Z = complete_ra(100, num_arms = 2), Y = rnorm(100))
fit <- lm_robust(Y ~ Z, data = dat)
prepped <- prep_fit(fit, term = "ZT2")
get_glance_df(prepped)
\dontshow{\}) # examplesIf}
}
\seealso{
Other component accessors:
\code{\link[=get_estimates_df]{get_estimates_df()}},
\code{\link[=get_vcov]{get_vcov()}}
}
\concept{component accessors}
