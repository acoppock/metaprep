% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_glance_df.R
\name{get_glance_df}
\alias{get_glance_df}
\title{Extract and tidy glance-level model summaries}
\usage{
get_glance_df(prepped_fits_df)
}
\arguments{
\item{prepped_fits_df}{A data frame or tibble containing a list-column
named \code{glance_obj}, where each element is a single-row data frame (as
returned by \code{\link[broom:reexports]{broom::glance()}}).}
}
\value{
A tibble containing model-level summary statistics, with one row per
model.
}
\description{
\code{get_glance_df()} takes a data frame of prepped model fits (as returned by
\code{\link[=prep_fit]{prep_fit()}}) and extracts the \code{glance_obj} list-column, unnesting it into
a tidy tibble with one row per fitted model.

This is typically used to compile model-level summary statistics (such as
R², adjusted R², residual standard error, etc.) across multiple fitted
models, while dropping unneeded columns like \code{tidy_obj} and \code{vcov_obj}.
}
\details{
\itemize{
\item The function performs defensive checks to ensure the input is a data frame
and that the \code{glance_obj} column exists and contains data frames.
\item Typically used after calling \code{\link[=prep_fit]{prep_fit()}} and optionally combining results
with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}.
}
}
\examples{
\dontrun{
library(estimatr)
library(dplyr)

# Example data
dat <- data.frame(
  Y = rnorm(100),
  Z = factor(sample(c("T0", "T1", "T2"), 100, TRUE)),
  X = rnorm(100)
)

# Fit models
fit_1 <- lm_robust(Y ~ Z + X, data = dat)
fit_2 <- lm_robust(Y ~ Z + X, data = dat)

# Prep results
prepped_fit_1 <- prep_fit(fit_1, term = c("ZT1", "ZT2"))
prepped_fit_2 <- prep_fit(fit_2, term = c("ZT1"))

# Combine
prepped_fits_df <- bind_rows(prepped_fit_1, prepped_fit_2, .id = "fit")

# Extract glance-level summaries
glance_df <- get_glance_df(prepped_fits_df)
glance_df
}

}
