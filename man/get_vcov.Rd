% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{get_vcov}
\alias{get_vcov}
\title{Extract variance-covariance matrix from prepped fits or estimates_vcov object}
\usage{
get_vcov(x, ...)
}
\arguments{
\item{x}{Either a prepped_fits tibble or an estimates_vcov object}

\item{...}{Additional arguments passed to methods}
}
\value{
A variance-covariance matrix (block-diagonal for prepped_fits)
}
\description{
Generic function to extract vcov matrix. Works with both:
\itemize{
\item \code{prepped_fits} tibbles (creates block-diagonal matrix)
\item \code{estimates_vcov} objects (extracts $vcov)
}
}
\examples{
\dontshow{if (requireNamespace("metafor", quietly = TRUE) && requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)
library(randomizr)
library(estimatr)

set.seed(123)
dat_1 <- data.frame(Z = complete_ra(50, num_arms = 2), Y = rnorm(50))
dat_2 <- data.frame(Z = complete_ra(100, num_arms = 3), Y = rnorm(100))

fit_1 <- lm_robust(Y ~ Z, data = dat_1)
fit_2 <- lm_robust(Y ~ Z, data = dat_2)

prepped_fits <- bind_rows(
  study_1 = prep_fit(fit_1, term = "ZT2"),
  study_2 = prep_fit(fit_2, term = c("ZT2", "ZT3")),
  .id = "study"
)
# Block-diagonal vcov across studies, ready for metafor
get_vcov(prepped_fits)

ev <- as_estimates_vcov(prepped_fits)
ev |> rma_mv_helper(yi = estimate, random = ~ 1 | id)
\dontshow{\}) # examplesIf}
}
