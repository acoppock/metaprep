% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rma_helpers.R
\name{rma_mv_helper}
\alias{rma_mv_helper}
\title{Run rma.mv on an estimates_vcov object}
\usage{
rma_mv_helper(object, yi, V = NULL, cluster = NULL, clubSandwich = TRUE, ...)
}
\arguments{
\item{object}{An estimates_vcov object}

\item{yi}{Bare column name of the estimates (e.g. \code{estimate}), or a two-sided
formula \code{estimate ~ moderators}. The formula form is metafor's own: it is
passed through unchanged, and \code{yi = estimate ~ x} fits what
\verb{yi = estimate, mods = ~ x} fits. Supplying both a formula and \code{mods} is an
error, since metafor would read the moderators off the formula and ignore
\code{mods}.}

\item{V}{Variance-covariance matrix (defaults to the vcov from object)}

\item{cluster}{Optional clustering variable for cluster-robust (sandwich)
standard errors. May be a bare column name of the estimates (e.g.
\code{cluster = study}), a string-named column via \code{.data[[var]]}, or an
external vector. When supplied, the fit is passed to
\code{\link[metafor:robust]{metafor::robust()}}; when \code{NULL} (default) the ordinary model-based fit is
returned.}

\item{clubSandwich}{Logical, passed to \code{\link[metafor:robust]{metafor::robust()}} when \code{cluster} is
supplied. \code{TRUE} (default) requests CR2 cluster-robust standard errors via
the clubSandwich package; \code{FALSE} uses metafor's CR0 estimator.}

\item{...}{Additional arguments passed to \code{\link[metafor:rma.mv]{metafor::rma.mv()}}, such as \code{random}, \code{mods}, etc.}
}
\value{
An object of class \code{rma.mv} as returned by \code{\link[metafor:rma.mv]{metafor::rma.mv()}}, or,
when \code{cluster} is supplied, a \code{robust.rma} object from \code{\link[metafor:robust]{metafor::robust()}}.
}
\description{
A convenience wrapper for \code{\link[metafor:rma.mv]{metafor::rma.mv()}} that automatically extracts
the estimates data frame and variance-covariance matrix from an estimates_vcov object.

This function passes all arguments directly to \code{\link[metafor:rma.mv]{metafor::rma.mv()}}, but
handles the \code{data} and \code{V} arguments automatically.
}
\details{
Estimates must be finite. \code{metafor} drops non-finite rows with a warning and
returns a fit whose \code{k} is smaller than the object, which silently misaligns
anything joining a per-estimate quantity such as \code{\link[stats:weights]{stats::weights()}} back onto
the estimates, so \code{rma_mv_helper()} errors instead and leaves the choice of
which estimates to drop to you. The same reasoning governs the non-finite
\code{vcov} guard applied when the object is built.
}
\examples{
\dontshow{if (requireNamespace("metafor", quietly = TRUE) && requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)
library(randomizr)
library(estimatr)

set.seed(123)
dat_1 <- data.frame(Z = complete_ra(50, num_arms = 2), Y = rnorm(50))
dat_2 <- data.frame(Z = complete_ra(100, num_arms = 3), Y = rnorm(100))
dat_3 <- data.frame(Z = complete_ra(200, num_arms = 4), Y = rnorm(200))

fit_1 <- lm_robust(Y ~ Z, data = dat_1)
fit_2 <- lm_robust(Y ~ Z, data = dat_2)
fit_3 <- lm_robust(Y ~ Z, data = dat_3)

prepped_fits <- bind_rows(
  study_1 = prep_fit(fit_1, term = "ZT2"),
  study_2 = prep_fit(fit_2, term = c("ZT2", "ZT3")),
  study_3 = prep_fit(fit_3, term = c("ZT2", "ZT3", "ZT4")),
  .id = "study"
)
ev <- as_estimates_vcov(prepped_fits)

ev |> rma_mv_helper(yi = estimate, random = ~ 1 | id)
ev |> rma_mv_helper(yi = estimate, mods = ~ study, random = ~ 1 | id)
ev |>
  filter(study != "study_1") |>
  rma_mv_helper(yi = estimate, random = ~ 1 | id)

# Cluster-robust (CR2) standard errors in one step (needs clubSandwich):
if (requireNamespace("clubSandwich", quietly = TRUE)) {
  ev |> rma_mv_helper(yi = estimate, random = ~ 1 | id, cluster = study)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=rma_uni_helper]{rma_uni_helper()}} when the estimates are genuinely independent, and
\link{estimates_vcov} for the object it reads from.

Other meta-analysis wrappers:
\code{\link[=rma_uni_helper]{rma_uni_helper()}}
}
\concept{meta-analysis wrappers}
