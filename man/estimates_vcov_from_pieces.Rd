% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov.R
\name{estimates_vcov_from_pieces}
\alias{estimates_vcov_from_pieces}
\title{Create estimates_vcov from separate estimates and vcov}
\usage{
estimates_vcov_from_pieces(estimates_df, vcov_matrix)
}
\arguments{
\item{estimates_df}{A data frame or tibble of coefficient estimates,
typically from \code{\link[=get_estimates_df]{get_estimates_df()}}.}

\item{vcov_matrix}{A variance-covariance matrix, typically from
\code{\link[=get_vcov]{get_vcov()}}. Must have dimensions matching the number of
rows in estimates_df.}
}
\value{
An object of class \code{estimates_vcov}
}
\description{
Alternative constructor for estimates_vcov objects when you already have
the estimates data frame and block-diagonal vcov matrix separately.

This is useful if you've already called \code{\link[=get_estimates_df]{get_estimates_df()}} and
\code{\link[=get_vcov]{get_vcov()}} and want to combine them into a synchronized object.
}
\examples{
\dontrun{
# If you already have separate pieces
estimates_df <- get_estimates_df(prepped_fits)
vcov_matrix <- get_vcov(prepped_fits)

# Combine them into an estimates_vcov object
ev <- estimates_vcov_from_pieces(estimates_df, vcov_matrix)

# Now you can use dplyr verbs with automatic vcov synchronization
ev |> filter(country == "USA")
}

}
