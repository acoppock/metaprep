% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates_vcov_class.R
\name{estimates_vcov}
\alias{estimates_vcov}
\alias{estimates_vcov-class}
\alias{print.estimates_vcov}
\alias{as.data.frame.estimates_vcov}
\alias{as_tibble.estimates_vcov}
\title{The estimates_vcov object}
\description{
An \code{estimates_vcov} object holds a set of coefficient estimates together with
the variance-covariance matrix that describes their dependence, and keeps the
two aligned as you reshape them. It is the object the rest of the package is
built around: \code{\link[=as_estimates_vcov]{as_estimates_vcov()}} and \code{\link[=make_estimates_vcov]{make_estimates_vcov()}} create one,
the \link{dplyr-methods} reshape one, and \code{\link[=rma_mv_helper]{rma_mv_helper()}} pools one.

The problem it solves is bookkeeping. A meta-analysis of multi-arm trials
needs a block-diagonal vcov whose row \code{i} corresponds to estimate \code{i}, and
every filter, sort, or subset of the estimates has to be applied to the matrix
in the same way. Doing that by hand is where a meta-analysis goes silently
wrong: nothing about a misaligned matrix looks wrong, and the pooled result is
simply the wrong number.
}
\section{Structure}{

The object is a list of three components:

\describe{
\item{\code{estimates}}{A tibble of estimates, one row each, with an \code{id} column
first. Carries whatever columns \code{\link[=prep_fit]{prep_fit()}} produced (\code{term}, \code{estimate},
\code{std.error}, and so on) plus any you have added.}
\item{\code{vcov}}{A square symmetric matrix, one row and column per estimate, in
the same order as the rows of \code{estimates}, with \code{dimnames} equal to the
\code{id} values.}
\item{\code{row_map}}{Internal bookkeeping. See "Public interface" below.}
}
}

\section{Public interface}{

\code{estimates} and \code{vcov} are part of the public interface. Read them however
suits the code you are writing: \code{ev$estimates} and \code{ev$vcov} directly, or
\code{\link[=get_estimates_df]{get_estimates_df()}} and \code{\link[=get_vcov]{get_vcov()}}. Both will keep working.

What is guaranteed about \code{vcov} is its \emph{content and shape}, not its storage
class: square, symmetric, finite, one row and column per estimate in the same
order as \code{estimates}, \code{dimnames} equal to \code{id}. Ordinary matrix operations
(\code{V[i, j]}, \code{V[idx, idx]}, \code{diag(V)}, \code{upper.tri(V)}, \code{w \%*\% V \%*\% w}) are the
supported way to use it, and they behave the same whether the matrix is stored
densely or sparsely. Code that tests the storage class itself, for instance
with \code{is.matrix()}, is relying on something the object does not promise.

\code{row_map} is \strong{internal} and may change without notice. It records which rows
of a parent object the current rows came from, but the verbs do not agree on
what "parent" means: \code{filter()} and the filtering joins set it to positions
within the object they were handed, while \code{arrange()} composes it through. Do
not build on it. Use \code{id} when you need a stable per-estimate label; it is
preserved through subsetting for exactly that purpose.
}

\section{The id column}{

Every object carries an \code{id} column, added when it is built, and the vcov's
\code{dimnames} match it. \code{id} is what links an estimate to its row and column of
the matrix, and it is preserved through subsetting, so a filtered object keeps
the ids it had rather than being renumbered from one. That is what makes \code{id}
usable as a random-effect grouping term: \code{random = ~ 1 | id} gives each
estimate its own random effect. (\code{\link[=bind_estimates_vcov]{bind_estimates_vcov()}} is the exception: it
renumbers \code{id} across the combined object, since the inputs each started
from one.)
}

\section{What stays synchronized, and what does not}{

The dplyr methods keep the vcov \strong{row-aligned}. Dropping or reordering
estimates drops or reorders the matching rows and columns of the matrix, so
\code{filter()}, \code{slice()}, \code{arrange()}, \code{semi_join()}, and \code{anti_join()} are all
safe. Verbs that cannot change the row set (\code{mutate()}, \code{select()}, \code{rename()},
\code{relocate()}) leave the matrix untouched. Verbs that would add or drop rows
unpredictably (\code{inner_join()}, \code{full_join()}, \code{right_join()}) are refused
rather than silently desynchronizing the object.

What the dplyr methods never do is \strong{transform} the matrix. \code{mutate()} can
change the values in the \code{estimate} column while leaving the vcov exactly as it
was, so \code{mutate(estimate = -estimate)} produces an object whose covariances no
longer describe its estimates. Use \code{\link[=rescale_estimates_vcov]{rescale_estimates_vcov()}} to sign-flip or
rescale, which updates the matrix to match.
}

\section{Guarantees}{

Every object that gets built satisfies three conditions, checked at
construction: the estimates and the vcov have the same number of rows; the
vcov is square and symmetric (asymmetry beyond floating-point noise is an
error, not something to average away); and the vcov is finite. A non-finite
covariance usually means a rank-deficient fit returned coefficients without
usable standard errors, and such an estimate must not be pooled as if it
carried uncertainty.
}

\examples{
library(dplyr)

set.seed(1)
dat_1 <- data.frame(Y = rnorm(60), Z = factor(rep(c("T0", "T1"), each = 30)))
dat_2 <- data.frame(Y = rnorm(90), Z = factor(rep(c("T0", "T1", "T2"), each = 30)))

ev <- as_estimates_vcov(bind_rows(
  study_1 = prep_fit(lm(Y ~ Z, dat_1), term = "ZT1"),
  study_2 = prep_fit(lm(Y ~ Z, dat_2), term = c("ZT1", "ZT2")),
  .id = "study"
))
ev

# The id column indexes the vcov
get_estimates_df(ev)$id
rownames(get_vcov(ev))

# Study 2's two arms covary; study 1 is independent of both
round(get_vcov(ev), 5)

# Subsetting keeps the two in step, and keeps the original ids
ev_2 <- filter(ev, study == "study_2")
get_estimates_df(ev_2)$id
dim(get_vcov(ev_2))

}
\seealso{
\code{\link[=as_estimates_vcov]{as_estimates_vcov()}} and \code{\link[=make_estimates_vcov]{make_estimates_vcov()}} to build one,
\link{dplyr-methods} for the verbs it supports, and \code{\link[=rma_mv_helper]{rma_mv_helper()}} to pool it.

Other estimates_vcov objects:
\code{\link[=as_estimates_vcov]{as_estimates_vcov()}},
\code{\link[=bind_estimates_vcov]{bind_estimates_vcov()}},
\code{\link{dplyr-methods}},
\code{\link[=make_estimates_vcov]{make_estimates_vcov()}},
\code{\link[=rescale_estimates_vcov]{rescale_estimates_vcov()}}
}
\concept{estimates_vcov objects}
