% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rma_helpers.R
\name{rma_uni_helper}
\alias{rma_uni_helper}
\title{Run rma.uni on an estimates_vcov object}
\usage{
rma_uni_helper(object, yi, vi = NULL, cluster = NULL, clubSandwich = TRUE, ...)
}
\arguments{
\item{object}{An estimates_vcov object}

\item{yi}{Formula or bare column name specifying the effect sizes (e.g., \code{estimate})}

\item{vi}{Numeric vector specifying the variances (defaults to diag(vcov))}

\item{cluster}{Optional bare column name (evaluated in the estimates data
frame, like \code{yi}) giving the clustering variable for cluster-robust
(sandwich) standard errors via \code{\link[metafor:robust]{metafor::robust()}}. \code{NULL} (default)
returns the ordinary model-based fit.}

\item{clubSandwich}{Logical, passed to \code{\link[metafor:robust]{metafor::robust()}} when \code{cluster} is
supplied. \code{TRUE} (default) requests CR2 standard errors via the
clubSandwich package; \code{FALSE} uses metafor's CR0 estimator.}

\item{...}{Additional arguments passed to \code{\link[metafor:rma.uni]{metafor::rma.uni()}}}
}
\value{
An object of class \code{rma.uni} as returned by \code{\link[metafor:rma.uni]{metafor::rma.uni()}}, or,
when \code{cluster} is supplied, a \code{robust.rma} object from \code{\link[metafor:robust]{metafor::robust()}}.
}
\description{
A convenience wrapper for \code{\link[metafor:rma.uni]{metafor::rma.uni()}} that automatically extracts
the estimates data frame and variance estimates from an estimates_vcov object.

Note: This function uses the diagonal of the vcov matrix as the variance
estimates. If you have correlated estimates (e.g., from multi-arm trials),
use \code{\link[=rma_mv_helper]{rma_mv_helper()}} instead to properly account for the correlation structure.
}
\details{
Taking the diagonal throws away every covariance the object carries, which
makes the pooled standard error too small. When \code{vi} is not supplied and the
vcov has nonzero off-diagonal entries, the function warns (with class
\code{"metaprep_discarded_covariance"}) naming how many covariances were dropped.
Supply \code{vi} explicitly when the univariate fit is genuinely what you want.

Estimates must be finite. \code{metafor} drops non-finite rows with a warning and
returns a fit whose \code{k} is smaller than the object, which silently misaligns
anything joining a per-estimate quantity such as \code{\link[stats:weights]{stats::weights()}} back onto
the estimates, so \code{rma_uni_helper()} errors instead and leaves the choice of
which estimates to drop to you.
}
\examples{
\dontshow{if (requireNamespace("metafor", quietly = TRUE) && requireNamespace("randomizr", quietly = TRUE) && requireNamespace("estimatr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)
library(randomizr)
library(estimatr)

set.seed(123)
dat_1 <- data.frame(Z = complete_ra(50, num_arms = 2), Y = rnorm(50))
dat_2 <- data.frame(Z = complete_ra(100, num_arms = 2), Y = rnorm(100))
dat_3 <- data.frame(Z = complete_ra(200, num_arms = 2), Y = rnorm(200))

fit_1 <- lm_robust(Y ~ Z, data = dat_1)
fit_2 <- lm_robust(Y ~ Z, data = dat_2)
fit_3 <- lm_robust(Y ~ Z, data = dat_3)

prepped_fits <- bind_rows(
  study_1 = prep_fit(fit_1, term = "ZT2"),
  study_2 = prep_fit(fit_2, term = "ZT2"),
  study_3 = prep_fit(fit_3, term = "ZT2"),
  .id = "study"
)
ev <- as_estimates_vcov(prepped_fits)

ev |> rma_uni_helper(yi = estimate)
ev |>
  mutate(large_study = study == "study_3") |>
  rma_uni_helper(yi = estimate, mods = ~ large_study)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=rma_mv_helper]{rma_mv_helper()}}, which uses the full vcov and is what dependent
estimates need.

Other meta-analysis wrappers:
\code{\link[=rma_mv_helper]{rma_mv_helper()}}
}
\concept{meta-analysis wrappers}
